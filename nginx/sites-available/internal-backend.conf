# Configuración para dominio interno de pruebas (internal-dev-sofia-chat.sofiacall.com)
# Este dominio apunta al slot inactivo para pruebas antes del switch
server {
    listen 443 ssl http2;
    server_name internal-dev-sofia-chat.sofiacall.com;

    # Certificados SSL (deben configurarse manualmente)
    ssl_certificate /etc/letsencrypt/live/internal-dev-sofia-chat.sofiacall.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/internal-dev-sofia-chat.sofiacall.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Headers de seguridad
    add_header X-Frame-Options DENY;
    ad
